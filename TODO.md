# TODO

## Chatbot (DSA tutor)
- [ ] Add backend route: `POST /api/chat` in `app.py` returning JSON `{reply, tags}`
- [ ] Implement rule-based response logic (keyword routing) with helpful explanations + clarifying prompts
- [ ] Add chatbot UI to `templates/index.html`
- [ ] Add frontend chat logic to `static/js/app.js` (render messages + call `/api/chat`)
- [ ] Add chatbot styles to `static/css/styles.css`
- [ ] Smoke test: run Flask app and verify UI + API responses

